[project]
name = "Raqib"
version = "1.11.0"
requires-python = ">=3.10,<3.14"
dependencies = [
    "dbt-core~=1.11.0",
    "dbt-snowflake~=1.11.0",
    "dbt-autofix~=0.18.0",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]